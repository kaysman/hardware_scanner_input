name: hardware_scanner_input_example
description: Demonstrates HardwareScannerInput in a real Flutter screen.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ^3.4.0
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  hardware_scanner_input:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
